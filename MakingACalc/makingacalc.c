#include <stdio.h>
main()
{
    int a,b,c;
    printf("Enter A Number (a): ");
    scanf("%d",&a);
    printf("Enter another Number (b): ");
    scanf("%d",&a);
    printf("a+b= %d",a+b);
    prinitf("a-b= %d",a-b);
    printf("a*b= %d",a*b);
    prinitf("a/b= %d",a/b);
}