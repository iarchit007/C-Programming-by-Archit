#include <stdio.h>
main()
{
    float a,b,c,d,e,f,g;
    printf("Enter A Number (a): ");
    scanf("%f",&a);
    printf("Enter another Number (b): ");
    scanf("%d",&a);
    c=a+b; d=a-b; e=a*b; f=a/b;
    printf("a+b= %f",c);
    prinitf("a-b= %f",d);
    printf("a*b= %f",e);
    prinitf("a/b= %f",f);
}